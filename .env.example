PORT=3000
# Incluye el prefijo global del backend (`app.setGlobalPrefix('api/v1')`).
# El HttpClient llama endpoints sin prefijo (`/habits`, `/auth/refresh`), así
# que sin `/api/v1` acá todo responde 404 y la app queda pegada en el login.
NEXT_PUBLIC_API_URL=http://localhost:3010/api/v1
NEXT_PUBLIC_APP_URL=http://localhost:${PORT}

# Posthog product analytics (opcional — si no se setea, el cliente NO se inicializa
# y todas las llamadas a `analytics.*` son no-op silenciosas. Ver
# docs/business/growth-roadmap.md#stack-de-analytics para la taxonomía completa).
#
# Default: aunque haya KEY seteada, Posthog NO se inicializa en `pnpm dev`
# para evitar polución de eventos de testing en el dashboard de producción y
# ruido en consola. Solo se inicializa en builds de producción.
#
# Para validar end-to-end desde dev (ej. confirmar que un evento llega
# correctamente), poner ENABLE_IN_DEV=true en `.env.local`. Por defecto NO se
# setea.
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
NEXT_PUBLIC_POSTHOG_ENABLE_IN_DEV=

# Playwright E2E (solo desarrollo — NUNCA producción)
# Debe coincidir con el TEST_AUTH_SECRET configurado en el backend (mínimo 32 chars).
E2E_BASE_URL=http://localhost:3000
E2E_API_URL=http://localhost:3010
TEST_AUTH_SECRET=
